<div class= "mt-5 container px-5 px-sm-0">
  <div class="row">
      <h3>ユーザー一覧</h3>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>ユーザーID</th>
            <th>名前</th>
            <th>メールアドレス</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @end_users.each do |f| %>
            <tr onclick='window.location="<%= '/admin/end_users/' + f.id.to_s %>"' role="link" >
              <td><%= f.id %></td>
              <td><%= f.name %></td>
              <td><%= f.email %></td>
              <td>
                <% if f.is_deleted %>
                  <span class="text-secondary">退会</span>
                <% else %>
                  <span class="text-success font-weight-bold">有効</span>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>
</div>
