 <div class='container mt-5'>
  <div class='row'>
    <% @tags.each do |f| %>
      <span class='mr-3'>
        <%= link_to f.name, tag_path(f.id) %>(<%= f.post_tags.count %>)
      </span>
    <% end %>
  </div>
</div>