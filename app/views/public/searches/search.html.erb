<div class='container my-5'>
  <h3><i class="fa-solid fa-magnifying-glass text-warning"></i>"<%= @word %>"の検索結果：<%= @posts.count %>件</h3>
  <div class='row'>
    <% if @posts.exists? %>
      <%= render 'public/posts/index', posts: @posts %>
    <% else %>
      <p>ヒットする投稿がありませんでした。他の単語で再検索してください。</p>
    <% end %>
  </div>
  <div class='row'>
    <div class='mx-auto mt-4'>
      <div class='pagination'><%= paginate @posts %></div>
    </div>
  </div>
</div>
