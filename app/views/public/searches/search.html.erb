<div class='container my-5'>
  <h3>"<%= @word %>"の検索結果：<%= @posts.count %>件</h3>
  <div class='row'>
    <% if @posts.exists? %>
      <%= render 'public/posts/index', posts: @posts %>
    <% else %>
      <p>ヒットする投稿がありませんでした。他の単語で再検索してください。</p>
    <% end %>
  </div>
</div>
