<%= form_with model:[post, post_comment], url: post_post_comments_path(post, post_comment), method: :post, local: false do |f| %>
  <%= f.text_area :body, rows:'2', placeholder: "コメントする", class: 'comment-textarea' %>
  <%= f.submit "送信する", class: 'btn btn-outline-secondary' %>
<% end %>